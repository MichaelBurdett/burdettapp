export interface UserCredential {
  email: string;
  password: string;
}

export interface UserProfile {
  fullName: string;
  email: string;
  displayName: string;
  lastLogin: string;
  created: string;
}
